<h1>Listing comments</h1>

<table>
  <tr>
    <th>User</th>
    <th>Message</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.user_id %></td>
    <td><%= comment.message %></td>
    <td><%= link_to 'Show', [@project, @task, comment] %></td>
    <td><%= link_to 'Edit', edit_project_task_comment_path(@project, @task, comment) %></td>
    <td><%= link_to 'Destroy', [@project, @task, comment], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment', new_project_task_comment_path(@project, @task) %>
